import React, { useRef, useEffect, useState } from "react";
import { Animated, View, StyleSheet, PanResponder, Text } from "react-native";

function Navigator({swipe})
{
    
}

const styles = StyleSheet.create({
    screen: {
        width: "100%",
        height: "100%"
    },
    screenContainer: {
        width: "100%",
        height: "100%"
    }
})

export default Navigator